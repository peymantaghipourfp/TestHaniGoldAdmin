# Review package Task 1 (no-git working-tree snapshot)
# File: lib/src/domain/inventory/widget/item_temp_detail_receive.widget.dart
# Change: split pickImageMobile into _showPickImageSheet + pickImageMobile(ImageSource)

## BEFORE (broken pattern - for reviewer reference)
Future<void> pickImageMobile() async {
  final ImageSource? source = await showModalBottomSheet<ImageSource>(...);
  // then pickImage / pickMultiImage after await
}

## AFTER (key methods)
  void _showPickImageSheet() {
    showModalBottomSheet(
      context: context,
      builder: (context) {
        return SafeArea(
          child: Container(
            decoration: BoxDecoration(
              color: AppColor.secondary200Color,
              borderRadius: BorderRadius.circular(15),
            ),
            child: Wrap(
              children: [
                ListTile(
                  leading: Icon(Icons.camera_alt, color: AppColor.textColor),
                  title: Text(
                    'Ø¯ÙˆØ±Ø¨ÛŒÙ†',
                    style: AppTextStyle.bodyText.copyWith(
                      fontSize: 16,
                      fontWeight: FontWeight.w700,
                    ),
                  ),
                  onTap: () {
                    Get.back();
                    pickImageMobile(ImageSource.camera);
                  },
                ),
                ListTile(
                  leading: Icon(Icons.photo_library, color: AppColor.textColor),
                  title: Text(
                    'Ú¯Ø§Ù„Ø±ÛŒ',
                    style: AppTextStyle.bodyText.copyWith(
                      fontSize: 16,
                      fontWeight: FontWeight.w700,
                    ),
                  ),
                  onTap: () {
                    Get.back();
                    pickImageMobile(ImageSource.gallery);
                  },
                ),
              ],
            ),
          ),
        );
      },
    );
  }

  Future<void> pickImageMobile(ImageSource source) async {
    recordId.value = uuid.v4();

    try {
      if (source == ImageSource.camera) {
        final XFile? photo = await _picker.pickImage(
          source: ImageSource.camera,
          imageQuality: 70,
        );

        if (photo == null) return;

        widget.image.add(photo);
        widget.recId?.call(recordId.value, List<XFile>.from(widget.image));
        setState(() {});
      }

      if (source == ImageSource.gallery) {
        final List<XFile> images = await _picker.pickMultiImage();

        if (images.isEmpty) return;

        widget.image.addAll(images);
        widget.recId?.call(recordId.value, List<XFile>.from(widget.image));
        setState(() {});
      }
    } catch (e, s) {
      AppLogger.e("pickImageMobile error:", e, s);
      Get.snackbar('Ø®Ø·Ø§', 'Ø§Ù…Ú©Ø§Ù† Ø§Ù†ØªØ®Ø§Ø¨ ØªØµÙˆÛŒØ± ÙˆØ¬ÙˆØ¯ Ù†Ø¯Ø§Ø±Ø¯');
    }
  }


## onTap call sites (grep)
42: Future<void> pickImageDesktop() async {
63: void _showPickImageSheet() {
86: pickImageMobile(ImageSource.camera);
100: pickImageMobile(ImageSource.gallery);
111: Future<void> pickImageMobile(ImageSource source) async {
138: AppLogger.e("pickImageMobile error:", e, s);
339: onTap: () => isMobile ? _showPickImageSheet() : pickImageDesktop(),
446: onTap: () => isMobile ? _showPickImageSheet() : pickImageDesktop(),
